"""Nutrition domain — intake parsing + (future) classify/policy.

Phase 1 mechanical move lands only the ``intake`` helper here (ex-
``health_agent_infra.intake.nutrition``). Classification, policy, and the
frozen domain schemas arrive in Phase 5.
"""
